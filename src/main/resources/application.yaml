spring:
  application:
    name: Electronic Deanery
  profiles:
    include: dev

  main:
    allow-circular-references: true
server:
  servlet:
    session:
      cookie:
        http-only: true
        same-site: strict
        secure: true
